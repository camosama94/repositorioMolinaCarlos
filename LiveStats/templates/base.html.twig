<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        <style>
            :root {
                --primary: #e75719;
                --primary-dark: #9c380d;
                --secondary: #241aa5;
                --dark: #08052f;
                --light: #d0d0d0;
            }
            body {
                background-color: var(--light);
                color: black;
            }
            .auth-container {
                background-color: #2c2c2c;
                border: 1px solid var(--secondary);
                border-radius: 10px;
                color: wheat;
            }
            .btn-custom {
                background-color: var(--primary);
                border-color: var(--primary-dark);
                color: white;
            }
            .btn-custom:hover {
                background-color: var(--primary-dark);
                border-color: var(--primary);
            }
            .form-control {
                background-color: white;
                border: 1px solid var(--secondary);
                color: black;
            }
            .form-control:focus {
                background-color: rgba(208, 208, 208, 0.2);
                border-color: var(--primary);
                color: black;
                box-shadow: 0 0 0 0.25rem rgba(231, 87, 25, 0.25);
            }
        </style>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% endblock %}
    </head>
    <body>
    <nav class="navbar position-relative" style="background-color: #ea7b40; font-weight: bold">
        <div class="container-fluid d-flex align-items-center justify-content-between">

            <!-- Logo a la izquierda -->
            <a class="navbar-brand d-flex align-items-center" href="#">
                <img src="/images/logo.png" alt="Logo" width="150" height="75" class="me-2">
            </a>

            <!-- Menú alineado a la derecha -->
            <div class="d-flex align-items-center">
                <ul class="navbar-nav mb-2 mb-lg-0 flex-row">
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {{ app.user.username }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown"
                                style="position: absolute; top: 100%; z-index: 1000;">
                                <li><a class="dropdown-item" href="{{ path('user_gestion') }}">Panel de usuario</a></li>
                                <li><a class="dropdown-item" href="{{ path('ruta_partidos') }}">Ver partidos</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                                {% if 'ROLE_ADMIN_LIGA' in app.user.roles %}
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('ruta_admin') }}">Panel de gestión</a></li>
                                {% endif %}
                            </ul>
                        </li>
                    {% else %}
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            OPCIONES
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown"
                            style="position: absolute; top: 100%; z-index: 1000;">
                            <li><a class="dropdown-item" href="{{ path('ruta_partidos') }}">Ver partidos</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_login') }}">Iniciar Sesión</a></li>
                        </ul>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>




    {% block body %}
    {% endblock %}

    </body>
</html>
